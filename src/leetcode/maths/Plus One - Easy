//https://leetcode.com/problems/plus-one/
//66. Plus One

//Time Complexity - O(n)
//Space complexity - Worst case O(n), Average/Best case - O(1)

//Such easy and simple solution, didn't occurred to my mind. I had a lengthy solution which could handle up to array size of 9.


class Solution {
    public int[] plusOne(int[] digits) {

    int n = digits.length;
    for(int i=n-1; i>=0; i--) {
        if(digits[i] < 9) {
            digits[i]++;
            return digits;
        }

        digits[i] = 0;
    }

    int[] newNumber = new int [n+1];
    newNumber[0] = 1;

    return newNumber;

    }
}